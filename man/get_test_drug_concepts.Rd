% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x_get_test.R
\name{get_test_drug_concepts}
\alias{get_test_drug_concepts}
\alias{get_test_drug_classes}
\title{Get Test Drug Concepts}
\usage{
get_test_drug_concepts(
  conn,
  conn_fun = "connectAthena()",
  schema = "omop_vocabulary",
  limit = 100,
  verbose = TRUE,
  render_sql = TRUE
)

get_test_drug_classes(
  conn,
  conn_fun = "connectAthena()",
  schema = "omop_vocabulary",
  limit = 100,
  verbose = TRUE,
  render_sql = TRUE
)
}
\description{
Get random records of RxNorm and RxNorm Extension Concepts that are:
\enumerate{
  \item Valid,
  \item Not a Class,
  \item Belong to the Drug domain
}

Get random records of ATC and HemOnc Drug Classes that are:
\enumerate{
  \item Valid,
  \item Class,
  \item Belong to the Drug domain
}
}
\examples{
library(chariot)
library(tidyverse)

# Get Unfiltered Test Concepts (Invalid Concepts are included)
get_test_concepts()

# The size of the resultset can be altered
get_test_concepts(limit = 25)

# Concepts specific to the Drug domain can be sampled
# RxNorm/RxNorm Extension Concepts
get_test_drug_concepts(limit = 10)

# HemOnc/ATC Classes
get_test_drug_classes(limit = 10)

# Test data can also be taken from tables other than Concept
get_test_data(table = "concept_relationship")
get_test_data(table = "concept_ancestor", limit = 10)
library(chariot)
library(tidyverse)

# Get Unfiltered Test Concepts (Invalid Concepts are included)
get_test_concepts()

# The size of the resultset can be altered
get_test_concepts(limit = 25)

# Concepts specific to the Drug domain can be sampled
# RxNorm/RxNorm Extension Concepts
get_test_drug_concepts(limit = 10)

# HemOnc/ATC Classes
get_test_drug_classes(limit = 10)

# Test data can also be taken from tables other than Concept
get_test_data(table = "concept_relationship")
get_test_data(table = "concept_ancestor", limit = 10)
}
\seealso{
\code{\link[rlang]{parse_expr}}
 \code{\link[SqlRender]{render}}

\code{\link[rlang]{parse_expr}}
 \code{\link[SqlRender]{render}}
}
